{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Backend",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "debugAdapter": "dlv-dap",
      "program": "${workspaceFolder}/backend/cmd/helpdesk",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "PATH": "${userHome}/.local/share/mise/shims:${env:PATH}"
      }
    }
  ]
}
